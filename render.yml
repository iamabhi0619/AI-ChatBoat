services:
  - type: web
    name: rasa-bot
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "rasa run --enable-api --cors '*' --port $PORT"
    runtime: python3.8
